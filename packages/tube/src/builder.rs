use crate::Slay3rTube;

pub struct Slay3rTubeBuilder {
    // TODO
}

impl Slay3rTubeBuilder {
    pub fn new() -> Self {
        Slay3rTubeBuilder {}
    }

    pub fn build(&self) -> Slay3rTube {
        todo!();
    }
}
