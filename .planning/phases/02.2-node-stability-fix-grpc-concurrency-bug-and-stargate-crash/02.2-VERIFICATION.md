---
phase: 02.2-node-stability-fix-grpc-concurrency-bug-and-stargate-crash
verified: 2026-03-20T19:30:00Z
status: passed
score: 10/10 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Run two concurrent grpcurl BroadcastTx calls while finalize_block is executing and observe neither returns an error"
    expected: "Both calls complete without error or timeout; the check_tx read lock does not block behind the finalize_block write lock"
    why_human: "Requires a live testnet with real concurrent load to observe the timing behavior; cargo tests run serially and cannot reproduce the contention scenario"
---

# Phase 02.2: Node Stability Verification Report

**Phase Goal:** Fix the gRPC concurrency bug (Mutex to RwLock migration) and eliminate node-crashing panics from unhandled CosmosMsg variants (Stargate crash fix).
**Verified:** 2026-03-20T19:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Concurrent gRPC queries execute without blocking behind finalize_block | VERIFIED | `self.app.read().await` in all gRPC handlers; `self.app.write().await` only in finalize_block (node.rs:190) |
| 2 | check_tx via BroadcastTx acquires a shared read lock, not an exclusive mutex | VERIFIED | grpc.rs:96 `let app = self.app.read().await` inside broadcast_tx handler |
| 3 | finalize_block acquires an exclusive write lock | VERIFIED | node.rs:190 `let mut app = self.app.write().await;  // EXCLUSIVE write lock — mutates state` |
| 4 | set_block_certificate acquires an exclusive write lock | VERIFIED | main.rs:181 `let mut app = self.app.write().await;  // EXCLUSIVE write lock — mutates storage` |
| 5 | Mempool remains behind Arc<Mutex<Mempool>> (unchanged) | VERIFIED | grpc.rs:54 `pub mempool: Arc<Mutex<Mempool>>`, node.rs:52 `mempool: Arc<Mutex<Mempool>>`, all mempool access uses `.lock().await` |
| 6 | CosmosMsg::Stargate returns an error instead of crashing the node | VERIFIED | keeper.rs:1163-1167 `CosmosMsg::Stargate { type_url, .. } => return Err(WasmError::UnsupportedCosmosMsg(...))` |
| 7 | CosmosMsg::Any returns an error instead of crashing the node | VERIFIED | keeper.rs:1168-1172 `CosmosMsg::Any(any_msg) => return Err(WasmError::UnsupportedCosmosMsg(...))` |
| 8 | Unknown BankMsg variants return an error instead of crashing the node | VERIFIED | keeper.rs:1039-1041 `other => return Err(WasmError::UnsupportedCosmosMsg(...))` for BankMsg wildcard |
| 9 | Unknown WasmMsg variants return an error instead of crashing the node | VERIFIED | keeper.rs:1112-1114 `other => return Err(WasmError::UnsupportedCosmosMsg(...))` for WasmMsg wildcard |
| 10 | /app/version query returns UnsupportedPath instead of crashing the node | VERIFIED | query.rs:85-87 `"version" => Err(QueryError::UnsupportedPath(format!("{}/version", QUERY_PATH_APP)))` |

**Score:** 10/10 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/slay3rd/src/grpc.rs` | RwLock-based gRPC service with shared reads | VERIFIED | Line 52: `pub app: Arc<RwLock<App<T>>>` in LayerGrpcService; line 279: `pub app: Arc<RwLock<App<T>>>` in CosmosQueryState; `app.read().await` in all query paths |
| `app/slay3rd/src/node.rs` | RwLock-based LayerNode with read/write segregation | VERIFIED | Line 50: `app: Arc<RwLock<App<T>>>` in LayerNode struct; line 81: constructor takes `Arc<RwLock<App<T>>>`; line 93: `app()` returns `Arc<RwLock<App<T>>>` |
| `app/slay3rd/src/main.rs` | App wrapped in Arc<RwLock> | VERIFIED | Line 55: `use tokio::sync::{Mutex, RwLock};`; line 128: `app: Arc<RwLock<App<T>>>` in LayerReporter; line 348: `Arc::new(RwLock::new(app))` |
| `packages/app/src/wasm/error.rs` | UnsupportedCosmosMsg error variant | VERIFIED | Lines 34-35: `#[error("Unsupported CosmosMsg type: {0}")] UnsupportedCosmosMsg(String)` |
| `packages/app/src/wasm/keeper.rs` | Graceful error returns for all unhandled CosmosMsg variants | VERIFIED | 5 occurrences of `WasmError::UnsupportedCosmosMsg` (lines 1039, 1112, 1164, 1169, 1175); zero `todo!()` or `unimplemented!()` remaining |
| `packages/cosmos/src/query.rs` | Graceful error for /app/version query | VERIFIED | Lines 85-87: `"version" => Err(QueryError::UnsupportedPath(...))` replacing former `todo!()` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/slay3rd/src/grpc.rs` | `app/slay3rd/src/node.rs` | Shared `Arc<RwLock<App<T>>>` type | VERIFIED | Both structs use identical `Arc<RwLock<App<T>>>` field type; `app_arc.clone()` passed from main.rs to both |
| `app/slay3rd/src/main.rs` | `app/slay3rd/src/grpc.rs` | `Arc::new(RwLock::new(app))` construction | VERIFIED | main.rs:348 `Arc::new(RwLock::new(app))` feeds into `LayerGrpcService { app: app_arc.clone(), ... }` and `CosmosQueryState { app: app_arc.clone(), ... }` |
| `packages/app/src/wasm/keeper.rs` | `packages/app/src/wasm/error.rs` | `WasmError::UnsupportedCosmosMsg` variant | VERIFIED | error.rs defines the variant; keeper.rs uses it at lines 1039, 1112, 1164, 1169, 1175 |
| `packages/app/src/wasm/keeper.rs` | `packages/app/src/error.rs` | `PulsarError` From<WasmError> conversion | VERIFIED | keeper.rs uses `.into()` to convert WasmError to PulsarError; existing `#[from] WasmError` in PulsarError handles the conversion |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| STAB-01 | 02.2-01 | gRPC query endpoints remain responsive under concurrent load — `Arc<RwLock<App<T>>>` allows shared query reads while `finalize_block` holds exclusive write lock | SATISFIED | All gRPC query paths use `.read().await`; finalize_block uses `.write().await` in separate scoped block; 27 tests pass |
| STAB-02 | 02.2-02 | `CosmosMsg::Stargate` returns graceful error, node does NOT crash | SATISFIED | keeper.rs:1163-1167: explicit `CosmosMsg::Stargate { type_url, .. }` arm returns `Err(WasmError::UnsupportedCosmosMsg(...))` |
| STAB-03 | 02.2-02 | `CosmosMsg::Any` and all other unhandled variants return graceful errors | SATISFIED | keeper.rs:1168-1178: explicit `CosmosMsg::Any(any_msg)` arm + wildcard `other =>` arm, both return `Err`; zero `todo!()` remaining |
| STAB-04 | 02.2-02 | `/app/version` query returns `QueryError::UnsupportedPath`, not a panic | SATISFIED | query.rs:85-87: `"version" => Err(QueryError::UnsupportedPath(...))` verified; zero `todo!()` in query.rs |
| STAB-05 | 02.2-01 | Concurrent `BroadcastTx` calls complete without error — shared read lock in `check_tx` does not block behind `finalize_block` write lock | SATISFIED | grpc.rs:96 uses `self.app.read().await` for check_tx; node.rs:190 uses `self.app.write().await` for finalize_block; both are separate, independently-acquirable lock modes |

No orphaned requirements found. All 5 STAB requirements claimed by plans 02.2-01 and 02.2-02 are accounted for and satisfied.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/slay3rd/src/main.rs` | 464 | `// Phase 3+ TODO: Serialize and load Sharing...` | Info | Pre-existing planning note; not introduced by Phase 02.2; does not affect RwLock migration |
| `packages/cosmos/src/query.rs` | 52, 72, 80, 101, 102 | `// FIXME:` comments | Info | All pre-existing; none relate to the version query fix or the `parse_app_query` function body |
| `packages/app/src/wasm/keeper.rs` | 63, 361, 882, 1009 | `// TODO:` and `// FIXME:` comments | Info | All pre-existing; `keeper.rs:1009` FIXME is in `instantiate2_address` (non-determinism), unrelated to cosmwasm_msg_to_layer |

No blockers or warnings introduced by Phase 02.2. All anti-patterns are pre-existing in files that were partially modified.

---

### Compilation and Test Results

| Check | Result |
|-------|--------|
| `cargo check -p slay3rd` | Finished — no errors (4 pre-existing warnings in layer-app) |
| `cargo check -p layer-app -p layer-cosmos` | Finished — no errors (4 pre-existing warnings) |
| `cargo test -p slay3rd --lib` | 27 passed; 0 failed |
| `cargo test -p layer-app --lib` | 39 passed; 0 failed |

---

### Human Verification Required

#### 1. Concurrent gRPC Read Contention Under Live Load

**Test:** Start a 3-node testnet. While blocks are being finalized, fire 10 concurrent `grpcurl` calls to `/cosmos.bank.v1beta1.Query/Balance` and 10 concurrent `BroadcastTx` calls.
**Expected:** All 20 calls complete without error. None timeout or return `unavailable`. The `finalize_block` write lock does not starve the query read locks.
**Why human:** Requires a live process with real concurrent goroutine scheduling. Cargo tests run serially and cannot exercise the RwLock contention timing that the bug was introduced to fix.

---

### Gaps Summary

No gaps found. All automated verifications passed.

---

## Commit History

| Commit | Description |
|--------|-------------|
| `cab4b29` | feat(02.2-01): migrate grpc.rs and node.rs from Mutex to RwLock for App<T> |
| `c23db47` | feat(02.2-01): migrate main.rs LayerReporter and app_arc construction to RwLock |
| `e87b73c` | fix(02.2-02): replace panic sites in cosmwasm_msg_to_layer with Err returns |
| `87edb44` | fix(02.2-02): replace version query todo!() with UnsupportedPath error |

All 4 commits verified present in git history.

---

_Verified: 2026-03-20T19:30:00Z_
_Verifier: Claude (gsd-verifier)_
