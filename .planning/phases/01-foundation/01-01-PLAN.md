---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - packages/abci/ (deleted)
  - app/slay3rd/ (deleted)
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "cargo build --workspace succeeds on all remaining crates (no abci, no slay3rd)"
    - "No tendermint, tendermint-abci, tendermint-proto, or tendermint-rpc declarations exist in workspace Cargo.toml"
    - "No layer-abci workspace dependency exists in workspace Cargo.toml"
    - "cargo test --workspace --lib passes on all remaining crates"
  artifacts:
    - path: "Cargo.toml"
      provides: "Clean workspace without tendermint deps or abci/slay3rd members"
      contains: "cosmwasm-vm"
  key_links:
    - from: "Cargo.toml"
      to: "packages/app/, packages/cosmos/, packages/proto/, packages/std/, packages/storage/, packages/golem/, contracts/*"
      via: "workspace members list"
      pattern: "members.*=.*\\[.*packages.*\\]"
---

<objective>
Remove all CometBFT/Tendermint dependencies and delete the ABCI integration layer so the workspace builds cleanly with only the library crates and contracts.

Purpose: FOUND-01 requires a clean workspace build. The `packages/abci/` and `app/slay3rd/` packages are the only direct consumers of tendermint crates. Deleting them and cleaning the workspace `[workspace.dependencies]` table eliminates all direct tendermint references. The `cosmrs` transitive dependency on tendermint is a known acceptable issue documented for Phase 3.

Output: A workspace that compiles and passes all library tests without any CometBFT/Tendermint binary or crate references.
</objective>

<execution_context>
@/Users/jacobhartnell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobhartnell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ABCI and slay3rd packages, clean workspace deps</name>
  <files>Cargo.toml, packages/abci/ (delete entire directory), app/slay3rd/ (delete entire directory)</files>
  <read_first>
    - Cargo.toml (workspace root — see current members and dependency declarations)
    - packages/abci/Cargo.toml (confirm only tendermint deps, no surprises)
    - app/slay3rd/Cargo.toml (confirm tendermint + cosmrs deps, understand what stays)
  </read_first>
  <action>
1. Delete the `packages/abci/` directory entirely (`rm -rf packages/abci/`).

2. Delete the `app/slay3rd/` directory entirely (`rm -rf app/slay3rd/`).

3. Edit the root `Cargo.toml`:
   a. Change the `members` line from:
      ```
      members = ["app/*", "contracts/*", "packages/*"]
      ```
      to:
      ```
      members = ["contracts/*", "packages/*"]
      ```
      (Remove `"app/*"` since slay3rd was the only app member and is now deleted.)

   b. Remove these workspace dependency lines entirely:
      ```toml
      layer-abci = { path = "./packages/abci" }
      tendermint = { version = "0.39.1", default-features = false }
      tendermint-abci = "0.39.1"
      tendermint-proto = "0.39.1"
      tendermint-rpc = "0.39.1"
      ```

   c. Keep these workspace dependencies (still used by packages/cosmos or packages/app):
      - `cosmrs = { version = "0.13.0", ... }` (used by packages/cosmos/src/tx.rs, pubkey.rs)
      - `cosmos-sdk-proto = { version = "0.18.0", ... }` (used by packages/cosmos and packages/proto)
      - All `cosmwasm-*` deps (used by packages/app)
      - All `layer-*` path deps except `layer-abci`
      - All other deps (tokio, tonic, tracing, serde, etc.)

   d. Keep the `cw20-base` workspace dep if present (needed for dev-dependencies in tests).

   NOTE: `cosmrs 0.13.0` transitively pulls in tendermint, but this is acceptable for Phase 1. The direct workspace declarations for tendermint crates are what must be removed. Run `cargo tree -i tendermint 2>&1 | head -20` after cleanup to document the transitive path. If `cargo audit` flags tendermint transitively via cosmrs, add a note but do NOT try to upgrade cosmrs in this plan.

4. Run `cargo build --workspace 2>&1` and fix any compilation errors. Expected issues:
   - If any remaining package has `layer-abci = { workspace = true }` in its own Cargo.toml, remove that dep line.
   - If any remaining package has `tendermint-proto = { workspace = true }` or similar, those workspace deps no longer resolve — remove from the offending Cargo.toml.
   - Check packages/proto/Cargo.toml specifically — it may have a `features = ["server"]` that references tendermint-proto. Remove if present, but keep the generated proto files in `packages/proto/src/protos/` untouched (they are standalone generated code).

5. Run `cargo test --workspace --lib 2>&1` and ensure all tests pass.
  </action>
  <verify>
    <automated>cd /Users/jacobhartnell/Dev/projects/Layer/layer-sdk && cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `packages/abci/` directory does not exist
    - `app/slay3rd/` directory does not exist
    - Cargo.toml does NOT contain `tendermint =`
    - Cargo.toml does NOT contain `tendermint-abci`
    - Cargo.toml does NOT contain `tendermint-proto`
    - Cargo.toml does NOT contain `tendermint-rpc`
    - Cargo.toml does NOT contain `layer-abci`
    - Cargo.toml members line does NOT contain `"app/*"`
    - `cargo build --workspace` exits 0
    - `cargo test --workspace --lib` exits 0
  </acceptance_criteria>
  <done>The workspace compiles and all library tests pass with packages/abci and app/slay3rd deleted and all tendermint workspace deps removed. cosmrs remains as a transitive dependency only (acceptable for Phase 1).</done>
</task>

<task type="auto">
  <name>Task 2: Audit workspace and document transitive tendermint status</name>
  <files>Cargo.toml (no changes expected — verification only)</files>
  <read_first>
    - Cargo.toml (verify state after Task 1)
  </read_first>
  <action>
1. Install `cargo-audit` if not already present: `cargo install cargo-audit`

2. Run `cargo tree -i tendermint 2>&1 | head -30` to identify any remaining transitive tendermint dependencies. Expected result: tendermint only appears transitively via `cosmrs 0.13.0`. Document the output.

3. Run `cargo audit 2>&1`. Evaluate the results:
   - If no critical advisories: FOUND-01 audit criterion is met.
   - If tendermint advisories appear (from cosmrs transitive dep): this is a known acceptable issue. The specific advisory IDs and their nature should be documented. Do NOT try to fix by upgrading cosmrs — that is Phase 3 work.
   - If OTHER critical advisories appear (non-tendermint): attempt to fix by upgrading the affected crate in `[workspace.dependencies]`. Run `cargo build --workspace` and `cargo test --workspace --lib` after any upgrade to confirm no regressions.

4. Run `cargo build --workspace 2>&1` one final time to confirm everything is clean.

5. Run `cargo test --workspace --lib 2>&1` one final time to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /Users/jacobhartnell/Dev/projects/Layer/layer-sdk && cargo build --workspace 2>&1 | tail -3 && cargo test --workspace --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0
    - `cargo test --workspace --lib` exits 0
    - `cargo tree -i tendermint` output shows tendermint only via cosmrs (transitive), no direct workspace dependency
    - `cargo audit` either passes clean OR only shows tendermint advisories from cosmrs transitive dep (documented as known acceptable)
  </acceptance_criteria>
  <done>Workspace builds and tests pass. Tendermint is confirmed removed as a direct dependency. Any remaining transitive tendermint presence via cosmrs is documented as acceptable for Phase 1 (Phase 3 replaces cosmrs entirely). cargo audit passes or has only documented acceptable advisories.</done>
</task>

</tasks>

<verification>
1. `cargo build --workspace` exits 0
2. `cargo test --workspace --lib` exits 0
3. `grep -c 'tendermint' Cargo.toml` returns 0 (no tendermint lines in workspace deps)
4. `ls packages/abci 2>&1` returns "No such file or directory"
5. `ls app/slay3rd 2>&1` returns "No such file or directory"
</verification>

<success_criteria>
- The workspace compiles without errors
- All library tests pass
- No direct tendermint/CometBFT crate declarations remain in the workspace Cargo.toml
- packages/abci and app/slay3rd are fully deleted
- cargo audit passes or has only documented acceptable advisories from cosmrs transitive dep
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
