---
phase: 02.1-functional-node
plan: 01
subsystem: grpc
tags: [tonic, grpc, cosmos-tx, layer-proto, layer-cosmos, layer-storage, p2p-config]

# Dependency graph
requires:
  - phase: 02-commonware-consensus
    provides: "LayerNode, App<T>, Mempool, BLS consensus foundation"
provides:
  - "PeerConfig struct with address+public_key fields for authenticated P2P"
  - "NodeConfig with data_dir field replacing rocksdb_path+wal_path, computed path methods"
  - "LayerGrpcService<T> implementing cosmos.tx.v1beta1.Service and layer.sync.v1.Query"
  - "handle_cosmos_query() helper for Cosmos SDK path dispatch"
  - "BroadcastTx handler wired through parse_cosmos_tx, check_tx, mempool.submit"
  - "State sync streaming (LatestSequence, CurrentState, ChangesSince) via SyncProvider"
  - "test_broadcast_tx_roundtrip validating parse error, check_tx rejection, and uninit guard"
affects:
  - "02.1-02: Plan 02 wires LayerGrpcService into main.rs tonic server"
  - "02.1-03: Uses config.wal_path() and config.app_data_path() for RocksDB storage"

# Tech tracking
tech-stack:
  added:
    - "layer-cosmos = { workspace = true } — parse_cosmos_tx + parse_cosmos_query + encode_cosmos_response"
    - "layer-proto = { workspace = true, features = [server] } — cosmos.tx.v1beta1 and layer.sync.v1 server traits"
    - "tonic = { workspace = true } — async_trait, Request/Response/Status types"
    - "tokio-stream = { workspace = true } — Stream trait for server streaming"
    - "futures = { workspace = true } — StreamExt for .map() on streams"
    - "cosmrs = { workspace = true } (dev) — secp256k1 tx construction for unit tests"
  patterns:
    - "LayerGrpcService<T: PersistentStorage> generic — same pattern as LayerNode; enables MemoryStore tests and RocksDB production"
    - "Lock-drop-then-await — never hold Arc<Mutex<App<T>>> across .await points"
    - "app.info().is_none() guard before check_tx — prevents panic on uninitialized App"
    - "chain_id captured at construction — avoid locking App just to read chain_id in every request"

key-files:
  created:
    - app/slay3rd/src/grpc.rs
  modified:
    - app/slay3rd/src/config.rs
    - app/slay3rd/src/lib.rs
    - app/slay3rd/src/main.rs
    - app/slay3rd/Cargo.toml

key-decisions:
  - "NodeConfig.data_dir replaces both rocksdb_path and wal_path — single field, computed subpaths via wal_path() and app_data_path() methods"
  - "PeerConfig struct with address+public_key for [[peers]] TOML table — required by authenticated P2P (Plan 02)"
  - "SyncProvider trait imported as layer_app::SyncProvider — the sync module is private; trait re-exported from lib.rs"
  - "Test account_number hardcoded as 17 to match FIXED_ACCOUNT_NUMBER in layer_cosmos::tx (not re-exported)"
  - "test_broadcast_tx_roundtrip validates error paths only (parse failure, check_tx rejection) since genesis has no funded accounts — sufficient to confirm wiring"

patterns-established:
  - "Pattern: LayerGrpcService<T> clone via #[derive(Clone)] with Arc fields — cheap to clone across tonic server tasks"
  - "Pattern: stream.map(|r: Result<T, String>| r.map_err(Status::internal)) for SyncProvider stream error mapping"

requirements-completed: []

# Metrics
duration: 45min
completed: 2026-03-20
---

# Phase 02.1 Plan 01: NodeConfig + gRPC Service Module Summary

**NodeConfig upgraded with PeerConfig/data_dir for authenticated P2P, and LayerGrpcService wired through cosmos tx parse, check_tx, and state sync streaming with BroadcastTx unit tests**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-03-20T00:00:00Z
- **Completed:** 2026-03-20T00:45:00Z
- **Tasks:** 2/2
- **Files modified:** 5

## Accomplishments

- NodeConfig restructured: `Vec<String>` peers replaced with `Vec<PeerConfig>`, `rocksdb_path` + `wal_path` fields replaced by `data_dir` with computed `wal_path()` and `app_data_path()` methods; TOML `[[peers]]` table parsing tested
- All new dependencies (layer-cosmos, layer-proto server, tonic, tokio-stream, futures) added to slay3rd/Cargo.toml; `cargo check -p slay3rd --features rocksdb` passes clean
- `LayerGrpcService<T>` created in grpc.rs implementing both cosmos.tx.v1beta1.Service (BroadcastTx + 8 unimplemented stubs) and layer.sync.v1.Query (LatestSequence, CurrentState, ChangesSince streaming)
- `handle_cosmos_query()` helper exported for Plan 02 tower layer dispatch
- `test_broadcast_tx_roundtrip` test confirms parse error → InvalidArgument, check_tx rejection → FailedPrecondition, and uninitialized node guard

## Task Commits

Each task was committed atomically:

1. **Task 1: Update config.rs and Cargo.toml dependencies** - `d0fa258` (feat)
2. **Task 2: Create grpc.rs — gRPC service module with unit tests** - `694e9ce` (feat)

## Files Created/Modified

- `app/slay3rd/src/config.rs` — PeerConfig struct, NodeConfig with data_dir, wal_path()/app_data_path() methods, updated tests
- `app/slay3rd/Cargo.toml` — layer-cosmos, layer-proto (server), tonic, tokio-stream, futures, cosmrs (dev)
- `app/slay3rd/src/grpc.rs` — LayerGrpcService<T>, BroadcastTx, SyncQuery impl, handle_cosmos_query(), unit tests
- `app/slay3rd/src/lib.rs` — added `pub mod grpc`
- `app/slay3rd/src/main.rs` — fixed `config.wal_path` field → `config.wal_path()` method call

## Decisions Made

- NodeConfig.data_dir replaces both rocksdb_path and wal_path as a single field with computed sub-paths — cleaner API, matches the "always-RocksDB" direction of Phase 02.1
- SyncProvider imported from `layer_app::SyncProvider` (not `layer_app::sync::SyncProvider`) because the `sync` module is private; the trait is re-exported from layer_app/src/lib.rs
- `test_broadcast_tx_roundtrip` tests error paths only — genesis has no funded accounts so check_tx always rejects valid-format txs; this is sufficient to confirm end-to-end handler wiring

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed main.rs field access to method call**
- **Found during:** Task 1 (config.rs refactor)
- **Issue:** `main.rs` line 268 used `config.wal_path` as a struct field; after removing the field and replacing with a method, the binary would fail to compile
- **Fix:** Changed `config.wal_path` to `config.wal_path()` in main.rs
- **Files modified:** `app/slay3rd/src/main.rs`
- **Verification:** `cargo check -p slay3rd` passes clean
- **Committed in:** d0fa258 (Task 1 commit)

**2. [Rule 3 - Blocking] Fixed SyncProvider import path**
- **Found during:** Task 2 (grpc.rs creation)
- **Issue:** Initial import `use layer_app::sync::SyncProvider` failed — `sync` module is private; trait is re-exported at crate root
- **Fix:** Changed to `use layer_app::SyncProvider`
- **Files modified:** `app/slay3rd/src/grpc.rs`
- **Verification:** `cargo check -p slay3rd` passes clean
- **Committed in:** 694e9ce (Task 2 commit)

**3. [Rule 3 - Blocking] Added explicit type annotations for stream map closures**
- **Found during:** Task 2 (grpc.rs streaming methods)
- **Issue:** Rust type inference could not determine closure parameter types in `stream.map(|r| ...)` for SyncProvider streams
- **Fix:** Added explicit types: `|r: Result<WriteData, String>|` and `|r: Result<BlockWrites, String>|`
- **Files modified:** `app/slay3rd/src/grpc.rs`
- **Verification:** `cargo check -p slay3rd` passes clean
- **Committed in:** 694e9ce (Task 2 commit)

---

**Total deviations:** 3 auto-fixed (all Rule 3 blocking compile issues)
**Impact on plan:** All auto-fixes required for correct compilation. No scope creep.

## Issues Encountered

- `FIXED_ACCOUNT_NUMBER` constant in `layer_cosmos::tx` is not re-exported from the crate root — used hardcoded value `17` in test (matches the constant). No test behavior impact.

## Next Phase Readiness

- Plan 02 can now import `LayerGrpcService` from `slay3rd::grpc` and wire it into `tonic::transport::Server` in main.rs
- `config.wal_path()` and `config.app_data_path()` methods are ready for Plan 03's RocksDB storage switch
- `PeerConfig` struct is ready for Plan 02's authenticated P2P peer registration

---
*Phase: 02.1-functional-node*
*Completed: 2026-03-20*

## Self-Check: PASSED

- FOUND: app/slay3rd/src/grpc.rs
- FOUND: app/slay3rd/src/config.rs
- FOUND: .planning/phases/02.1-functional-node/02.1-01-SUMMARY.md
- FOUND: commit d0fa258 (Task 1: config.rs + Cargo.toml)
- FOUND: commit 694e9ce (Task 2: grpc.rs + lib.rs)
